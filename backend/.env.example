# Используется переменной окружения DATABASE_URL.
# Пример (Docker Postgres из docker-compose.yml):
DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/mobile_course

# Опционально: засыпка демо-товаров при первом старте (true/false).
SEED_DEMO_DATA=true
